export default function Run() {
  return (
    <section>
      <h1>Run</h1>
      <p>Live stream, tokens, cost, artifacts. Open a run via the API <code>GET /runs/:id/stream</code> or the Fastify pane <code>/ui/run</code>.</p>
    </section>
  );
}
